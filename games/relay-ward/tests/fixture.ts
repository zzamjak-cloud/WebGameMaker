import project from "../game.project.json" with { type: "json" };
import scene from "../scenes/main.scene.json" with { type: "json" };

export const relayWardBundle = {
  project,
  scenes: [scene],
};
